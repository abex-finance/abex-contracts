# ABEx Native Feeder (Only for test)

## Deployment

```bash
sui client --client.config ~/.sui/sui_config/client.yaml publish --gas-budget 100000
```

## Create Feeder

```bash
cd scripts
chmod a+x create_feeder.sh
./create_feeder.sh
```
